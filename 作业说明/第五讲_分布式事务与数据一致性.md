# 第五讲：分布式事务 —— TCC 补偿型事务

## 一、作业要求

### 场景描述
在商品秒杀系统中，订单服务和库存服务是两个独立的微服务，分别有各自数据库。

### 具体要求
1. 在秒杀下单时，基于 Redis 实现库存预扣减，防超卖、限购
2. 采用基于消息的一致性或 TCC 事务保障数据一致性
   - 下单 + 库存扣减一致性
   - 订单支付 + 订单状态更新一致性

## 二、分布式事务方案选型

### 2.1 方案对比

| 方案 | 一致性 | 性能 | 复杂度 | 适用场景 |
|------|--------|------|--------|---------|
| 2PC/XA | 强一致 | 差（同步阻塞） | 高 | 传统数据库 |
| **TCC** | **强一致** | **较好** | **中等** | **电商/金融** |
| 消息事务 | 最终一致 | 高 | 低 | 高并发异步 |

### 2.2 本系统选择 TCC 的原因

1. `seckill_product` 表已有 `locked_stock` 字段，天然适合 TCC 三阶段库存语义
2. 秒杀场景对库存准确性要求极高（不能超卖、不能漏单）
3. TCC 通过 Try 预留 + Confirm/Cancel 明确的状态机，保证强一致性
4. 与现有 Redis 预扣库存 + Kafka 异步架构无缝衔接

## 三、TCC 整体设计

### 3.1 架构概览
![alt text](../说明图片/第五讲-tcc设计.svg)

### 3.2 两个 TCC 参与者

| 参与者 | Try | Confirm | Cancel |
|--------|-----|---------|--------|
| **库存参与者** | `avail_stock -= qty, locked_stock += qty`（乐观锁） | `locked_stock -= qty`（永久消费） | `locked_stock -= qty, avail_stock += qty`（释放预留） |
| **订单参与者** | INSERT 订单 `status=-1 TRYING`, `timeout_at=now+15min` | `status: -1 -> 0 PENDING` | `status: -1 -> 2 CANCELLED` |

### 3.3 订单状态机
![alt text](../说明图片/第五讲-订单状态.svg)


## 四、TCC 核心实现

### 4.1 TCC 库存参与者

**文件**：`backend/src/main/java/com/seckill/service/tcc/TccStockParticipant.java`

```java
@Component
public class TccStockParticipant {

    // Try: 预留库存 (avail_stock -> locked_stock)
    public boolean tryReserve(Long productId, int quantity) {
        SeckillProduct sp = seckillProductMapper.findById(productId);
        // 乐观锁扣减，version不匹配则更新0行
        int updated = seckillProductMapper.decreaseStock(productId, quantity, sp.getVersion());
        return updated > 0;
    }

    // Confirm: 永久扣减 (locked_stock -= quantity)
    public boolean confirm(Long productId, int quantity) {
        SeckillProduct sp = seckillProductMapper.findById(productId);
        int updated = seckillProductMapper.confirmStock(productId, quantity, sp.getVersion());
        return updated > 0;
    }

    // Cancel: 释放预留 (locked_stock -> avail_stock)
    public boolean cancel(Long productId, int quantity) {
        SeckillProduct sp = seckillProductMapper.findById(productId);
        int updated = seckillProductMapper.cancelStock(productId, quantity, sp.getVersion());
        return updated > 0;
    }
}
```

**关键 SQL**：

```sql
-- Try: 预留库存
UPDATE seckill_product
SET avail_stock = avail_stock - #{quantity},
    locked_stock = locked_stock + #{quantity},
    version = version + 1
WHERE id = #{id} AND avail_stock >= #{quantity} AND version = #{version}

-- Confirm: 永久扣减
UPDATE seckill_product
SET locked_stock = locked_stock - #{quantity},
    version = version + 1
WHERE id = #{id} AND locked_stock >= #{quantity} AND version = #{version}

-- Cancel: 释放预留
UPDATE seckill_product
SET locked_stock = locked_stock - #{quantity},
    avail_stock = avail_stock + #{quantity},
    version = version + 1
WHERE id = #{id} AND locked_stock >= #{quantity} AND version = #{version}
```

### 4.2 TCC 订单参与者

**文件**：`backend/src/main/java/com/seckill/service/tcc/TccOrderParticipant.java`

```java
@Component
public class TccOrderParticipant {

    // Try: 创建临时订单 (status=TRYING, timeout_at=now+15min)
    public Order tryCreateOrder(Long userId, Long productId, int productType,
                                String productName, int quantity,
                                BigDecimal unitPrice, int timeoutMinutes) {
        // 幂等检查
        if (orderMapper.countSeckillOrder(userId, productId) > 0) return null;

        Order order = new Order();
        order.setOrderNo(snowflakeIdGenerator.nextId());
        order.setStatus(Order.STATUS_TRYING);          // -1
        order.setTimeoutAt(LocalDateTime.now().plusMinutes(timeoutMinutes));
        orderMapper.insert(order);
        return order;
    }

    // Confirm: TRYING -> PENDING（幂等）
    public boolean confirm(String orderNo) {
        Order order = orderMapper.findByOrderNo(orderNo);
        if (order.getStatus() == Order.STATUS_PENDING) return true;  // 幂等
        return orderMapper.confirmOrderStatus(order.getId(),
                Order.STATUS_TRYING, Order.STATUS_PENDING) > 0;
    }

    // Cancel: TRYING -> CANCELLED（幂等）
    public boolean cancel(String orderNo) {
        Order order = orderMapper.findByOrderNo(orderNo);
        if (order.getStatus() == Order.STATUS_CANCELLED) return true;  // 幂等
        return orderMapper.confirmOrderStatus(order.getId(),
                Order.STATUS_TRYING, Order.STATUS_CANCELLED) > 0;
    }
}
```

### 4.3 TCC 事务协调器

**文件**：`backend/src/main/java/com/seckill/service/tcc/TccTransactionCoordinator.java`

```java
@Component
public class TccTransactionCoordinator {

    public static final int DEFAULT_TIMEOUT_MINUTES = 15;

    // Try 阶段（Kafka消费者中调用）
    @Transactional
    public Order tryPhase(Long userId, Long productId, int quantity,
                          String productName, BigDecimal unitPrice) {
        // Step 1: 库存 Try（预留）
        if (!stockParticipant.tryReserve(productId, quantity)) return null;

        // Step 2: 订单 Try（创建TRYING订单）
        Order order = orderParticipant.tryCreateOrder(userId, productId, 1,
                productName, quantity, unitPrice, DEFAULT_TIMEOUT_MINUTES);
        if (order == null) {
            stockParticipant.cancel(productId, quantity);  // 回滚库存
            return null;
        }
        return order;
    }

    // Confirm 阶段（支付触发）
    @Transactional
    public boolean confirmPhase(String orderNo, Long productId, int quantity) {
        if (!orderParticipant.confirm(orderNo)) return false;
        if (!stockParticipant.confirm(productId, quantity)) return false;
        return true;
    }

    // Cancel 阶段（取消/超时触发）
    @Transactional
    public boolean cancelPhase(String orderNo, Long productId, int quantity) {
        if (!orderParticipant.cancel(orderNo)) return false;
        if (!stockParticipant.cancel(productId, quantity)) return false;
        redisUtils.increment("seckill:stock:" + productId, quantity);  // Redis回滚
        return true;
    }
}
```

## 五、TCC 在秒杀流程中的整合

### 5.1 完整时序

```
用户发起秒杀
  │
  ▼
SeckillServiceImpl.doSeckill()
  ├── Redis DECR 预扣库存（<1ms）
  ├── Redis SETNX 幂等标记（<1ms）
  ├── Kafka 发送消息
  └── 返回 "PROCESSING"
  │
  ▼
KafkaOrderConsumerService.handleSeckillOrder()
  ├── DB 幂等检查（countSeckillOrder）
  └── TccTransactionCoordinator.tryPhase()
       ├── 库存 Try: avail_stock -> locked_stock（乐观锁）
       └── 订单 Try: INSERT status=-1 TRYING, timeout_at=now+15min
  │
  ▼
订单 status = TRYING(-1)，等待用户操作
  │
  ├── 用户支付 ──→ payOrder()
  │                 └── TccTransactionCoordinator.confirmPhase()
  │                      ├── 订单 Confirm: -1 -> 0 PENDING
  │                      └── 库存 Confirm: locked_stock -= qty
  │                 └── updateStatus(1) PAID
  │
  ├── 用户取消 ──→ cancelOrder()
  │                 └── TccTransactionCoordinator.cancelPhase()
  │                      ├── 订单 Cancel: -1 -> 2 CANCELLED
  │                      ├── 库存 Cancel: locked_stock -> avail_stock
  │                      └── Redis INCR 回滚
  │
  └── 超时(15min) ──→ SeckillOrderTimeoutTask（每30秒扫描）
                       └── TccTransactionCoordinator.cancelPhase()
                            ├── 订单 Cancel: -1 -> 2 CANCELLED
                            ├── 库存 Cancel: locked_stock -> avail_stock
                            └── Redis INCR 回滚
```

### 5.2 Kafka 消费者改造

**改造前**：直接 `decreaseStock` + `insert order(status=0)` 在一个事务中完成

**改造后**：调用 `tccCoordinator.tryPhase()` 完成 TCC Try 阶段

```java
@KafkaListener(topics = "seckill-order", groupId = "seckill-order-group")
public void handleSeckillOrder(ConsumerRecord<String, SeckillOrderMessage> record,
                               Acknowledgment acknowledgment) {
    // 幂等检查
    if (orderMapper.countSeckillOrder(userId, productId) > 0) {
        acknowledgment.acknowledge();
        return;
    }
    // TCC Try（事务由协调器管理）
    Order order = tccCoordinator.tryPhase(userId, productId, quantity, name, price);
    if (order == null) {
        rollbackRedisStock(productId, quantity);
        acknowledgment.acknowledge();
        return;
    }
    acknowledgment.acknowledge();
}
```

### 5.3 支付/取消改造

**payOrder**：秒杀订单 TRYING 状态 → `confirmPhase()` → `updateStatus(PAID)`

**cancelOrder**：
- TRYING 状态 → `cancelPhase()`（TCC Cancel）
- PENDING 状态（已confirm）→ 直接取消 + 补偿性释放库存

## 六、超时自动取消机制

### 6.1 定时任务

**文件**：`backend/src/main/java/com/seckill/task/SeckillOrderTimeoutTask.java`

```java
@Scheduled(fixedDelay = 30000, initialDelay = 60000)  // 每30秒，启动60秒后首次执行
public void cancelTimeoutOrders() {
    List<Order> timeoutOrders = orderMapper.findTimeoutTryingOrders();
    for (Order order : timeoutOrders) {
        tccCoordinator.cancelPhase(order.getOrderNo(),
                order.getProductId(), order.getQuantity());
    }
}
```

### 6.2 超时查询 SQL

```sql
SELECT * FROM `order`
WHERE status = -1
  AND timeout_at IS NOT NULL
  AND timeout_at < NOW()
LIMIT 100
```

### 6.3 为什么需要超时机制

TCC 的 Try 阶段预留了库存（`locked_stock`），如果用户既不支付也不取消，库存将永远被锁定。超时机制确保在 `timeout_at`（默认15分钟）后自动执行 Cancel，释放被锁定的库存。

## 七、防超卖与数据一致性

### 7.1 四层防线

| 防线 | 位置 | 机制 | 作用 |
|------|------|------|------|
| 第一层 | Redis | `DECR` 原子操作 | 挡住99%无效请求 |
| 第二层 | Redis | `SETNX` 幂等标记 | 防止同一用户重复下单 |
| 第三层 | MySQL | TCC Try 乐观锁 | `avail_stock >= qty AND version = ?` |
| 第四层 | MySQL | TCC Confirm 乐观锁 | `locked_stock >= qty AND version = ?` |

### 7.2 TCC 幂等性保证

| 阶段 | 幂等机制 |
|------|---------|
| Try | DB 层 `countSeckillOrder` 排除 TRYING 和 CANCELLED |
| Confirm | 状态检查：已是 PENDING 则直接返回成功 |
| Cancel | 状态检查：已是 CANCELLED 则直接返回成功 |
| 超时取消 | `confirmOrderStatus` 带 `WHERE status = -1` 条件 |

### 7.3 一致性总结

```
成功路径:
  Redis DECR -> SETNX -> Kafka -> TCC Try(库存预留+TRYING订单)
  -> 用户支付 -> TCC Confirm(库存扣减+PENDING) -> 标记PAID

取消路径:
  Redis DECR -> SETNX -> Kafka -> TCC Try
  -> 用户取消/超时 -> TCC Cancel(库存释放+CANCELLED) -> Redis INCR

失败路径:
  Redis DECR -> SETNX -> Kafka -> TCC Try失败 -> Redis INCR回滚 -> Kafka重试

库存数据流:
  总库存 = avail_stock + locked_stock（Try阶段）
  总库存 = 0 + locked_stock（Confirm后，库存永久消费）
  总库存 = avail_stock + 0（Cancel后，库存恢复可用）
```

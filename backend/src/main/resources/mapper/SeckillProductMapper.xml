<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.mapper.SeckillProductMapper">

    <resultMap id="SeckillRM" type="com.seckill.model.entity.SeckillProduct">
        <id     property="id"           column="id"/>
        <result property="name"         column="name"/>
        <result property="description"  column="description"/>
        <result property="originPrice"  column="origin_price"/>
        <result property="seckillPrice" column="seckill_price"/>
        <result property="totalStock"   column="total_stock"/>
        <result property="availStock"   column="avail_stock"/>
        <result property="lockedStock"  column="locked_stock"/>
        <result property="version"      column="version"/>
        <result property="imageUrl"     column="image_url"/>
        <result property="startTime"    column="start_time"/>
        <result property="endTime"      column="end_time"/>
        <result property="status"       column="status"/>
        <result property="createdAt"    column="created_at"/>
        <result property="updatedAt"    column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="SeckillRM">
        SELECT * FROM seckill_product WHERE id = #{id}
    </select>

    <select id="findAllActive" resultMap="SeckillRM">
        SELECT * FROM seckill_product
        WHERE status = 1
        ORDER BY start_time ASC
    </select>

    <!-- TCC Try: 乐观锁预留库存 (avail_stock -> locked_stock)，version 不匹配则更新0行 -->
    <update id="decreaseStock">
        UPDATE seckill_product
        SET avail_stock  = avail_stock  - #{quantity},
            locked_stock = locked_stock + #{quantity},
            version      = version + 1
        WHERE id = #{id}
          AND avail_stock >= #{quantity}
          AND version     = #{version}
    </update>

    <!-- TCC Confirm: 确认扣减，locked_stock 转为已消费，version+1 -->
    <update id="confirmStock">
        UPDATE seckill_product
        SET locked_stock = locked_stock - #{quantity},
            version      = version + 1
        WHERE id = #{id}
          AND locked_stock >= #{quantity}
          AND version      = #{version}
    </update>

    <!-- TCC Cancel: 释放预留，locked_stock 回退到 avail_stock，version+1 -->
    <update id="cancelStock">
        UPDATE seckill_product
        SET locked_stock = locked_stock - #{quantity},
            avail_stock  = avail_stock  + #{quantity},
            version      = version + 1
        WHERE id = #{id}
          AND locked_stock >= #{quantity}
          AND version      = #{version}
    </update>

</mapper>
